name: Empaquetar AppImage (Portable)
on:
  push:
    branches: [ appimage-rebase, master ]
  workflow_dispatch:
  pull_request:
    branches: [ appimage-rebase, master ]

jobs:
  package:
    runs-on: ubuntu-22.04

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4

      - name: Instalar dependencias del sistema
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            ca-certificates build-essential git curl cmake clang ninja-build \
            qt6-base-dev qt6-base-dev-tools qt6-declarative-dev qt6-webengine-dev libqt6svg6-dev qt6-tools-dev \
            libcurl4-openssl-dev libssl-dev libasound2-dev libpulse-dev libjack-jackd2-dev \
            libpipewire-0.3-dev libsndio-dev libx11-dev libxi-dev libxext-dev libxfixes-dev \
            libxcursor-dev libxrandr-dev libxss-dev libxtst-dev libgl1-mesa-dev libegl1-mesa-dev \
            libgles2-mesa-dev libvulkan-dev vulkan-validationlayers libdrm-dev libgbm-dev \
            libudev-dev libevdev-dev libusb-1.0-0-dev libdbus-1-dev libbluetooth-dev bluez \
            libibus-1.0-dev libxkbcommon-dev libpng-dev libzip-dev libcups2-dev \
            libwayland-dev libunwind-dev libdecor-0-dev \
            libfuse2 wget file

      - name: Preparar estructura AppDir
        run: |
          mkdir -p APPIMAGE-TRINITY
          curl -L https://github.com/Trinity-LA/Trinity-Launcher/releases/download/2.6-beta/trinity-files.tar.gz -o trinity-files.tar.gz
          tar -xvf trinity-files.tar.gz -C APPIMAGE-TRINITY/
          
          # Limpieza y permisos
          rm -f APPIMAGE-TRINITY/usr/bin/mcpelauncher-webview
          chmod +x APPIMAGE-TRINITY/usr/bin/*

      - name: Integrar Librerías Físicas
        run: |
          mkdir -p APPIMAGE-TRINITY/usr/lib
          LIBZIP_REAL=$(find /usr/lib/x86_64-linux-gnu -name "libzip.so*" -type f | head -n 1)
          cp -v "$LIBZIP_REAL" APPIMAGE-TRINITY/usr/lib/libzip.so.5
          # Enlace simbólico interno para el sistema de compilación
          sudo ln -sf $PWD/APPIMAGE-TRINITY/usr/lib/libzip.so.5 /usr/lib/x86_64-linux-gnu/libzip.so.5

      - name: Descargar herramientas de empaquetado
        run: |
          wget https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          wget https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-x86_64.AppImage
          chmod +x linuxdeploy*.AppImage

      - name: Ejecutar empaquetado
        run: |
          export ARCH=x86_64
          export QMAKE=/usr/lib/qt6/bin/qmake
          export LD_LIBRARY_PATH=$PWD/APPIMAGE-TRINITY/usr/lib:$LD_LIBRARY_PATH
          
          # Crear un script envoltura (wrapper) en lugar de sobrescribir AppRun.
          # linuxdeploy usará este wrapper como el ejecutable principal.
          cat << 'EOF' > APPIMAGE-TRINITY/usr/bin/trinity-wrapper
          #!/bin/sh
          HERE="$(dirname "$(readlink -f "$0")")"
          export LD_LIBRARY_PATH="$HERE/../lib:$HERE/../lib/x86_64-linux-gnu:$LD_LIBRARY_PATH"
          export QML2_IMPORT_PATH="$HERE/../qml"
          export QML_IMPORT_PATH="$HERE/../qml"
          export QT_QPA_PLATFORM="xcb"
          export GDK_BACKEND="x11"
          export DISABLE_WAYLAND=1
          export MCPELAUNCHER_DATA_DIR="$HERE/../share/mcpelauncher"
          export PATH="$HERE:$PATH"
          cd "$HERE"
          exec ./trinity "$@"
          EOF
          chmod +x APPIMAGE-TRINITY/usr/bin/trinity-wrapper

          # Asegurarse de que el archivo desktop apunte al wrapper
          sed -i 's/^Exec=.*/Exec=trinity-wrapper/' APPIMAGE-TRINITY/usr/share/applications/com.trench.trinity.launcher.desktop

          # Ejecutar linuxdeploy en un solo paso, apuntando al wrapper y generando la salida
          ./linuxdeploy-x86_64.AppImage \
            --appdir APPIMAGE-TRINITY \
            -e APPIMAGE-TRINITY/usr/bin/trinity-wrapper \
            -e APPIMAGE-TRINITY/usr/bin/mcpelauncher-extract \
            -d APPIMAGE-TRINITY/usr/share/applications/com.trench.trinity.launcher.desktop \
            -i APPIMAGE-TRINITY/usr/share/icons/hicolor/scalable/apps/com.trench.trinity.launcher.svg \
            --plugin qt \
            --output appimage

      - name: Subir AppImage resultante
        uses: actions/upload-artifact@v4
        with:
          name: Trinity-Launcher-AppImage
          path: "*.AppImage"
